{
	"changes": "* Fixed: iOS launch image would flicker when transitioning into manual hiding phase.\n* Fixed: iOS WebView would sometimes fail to render after hiding launch image.",
	"description": "Images to display while your app is loading.\n\nSet the image to be displayed in the short time between launching your app and the webview being loaded and ready for the user.",
	"min_platform_version": "v2.5.1",
	"namespace": "launchimage",
	"platform_version": "v2.5.1",
	"version": "2.12"
}
